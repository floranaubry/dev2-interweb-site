# npm configuration (minimal)
# IMPORTANT: This repo uses pnpm. Run: corepack enable && pnpm install
# pnpm-specific settings are in .pnpmrc

# Shared settings that work with both npm and pnpm:
# (none currently needed)
